'use strict';

/**
 * @ngdoc module
 * @name fa.main
 * @description
 *
 * The `fa.main` module adds the application's main landing page.
 */

var angular = require('angular');
var uiRouter = require('angular-ui-router');

module.exports = angular.module('fa.main', [uiRouter]);
